## 0.1.0

- `Motion` widget with named variants
- `MotionValues`: opacity, x, y, scale
- `MotionTransition` with `delayChildren` and `staggerChildren`
- Child variant inheritance via `MotionScope`
- Reduced motion support
- Flutter web example (developer tools theme)
